import { Button } from "antd";

export const WithoutImage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        height: "50vh",
        padding: "0 1rem 0 1rem",
      }}
    >
      <h2>Sorry, We don't found images.</h2>
    </div>
  );
};
